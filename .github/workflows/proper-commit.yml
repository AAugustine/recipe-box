name: Verify Commit Message
on: push
jobs:
  log-github-events-stuff:
    name: "Log Head commit"
    runs-on: ubuntu-latest
    steps:
      - name: Logging event head commit
        run: |
         echo "${{toJSON(github.event.head_commit)}}"
